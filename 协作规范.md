# 协作规范

## 分支策略

### main（主分支）
- **用途**：生产就绪代码，始终保持可部署状态
- **推送条件**：前后端联调无误后，由 dev 合并至 main
- **禁止**：直接向 main 提交代码

### dev（开发分支）
- **用途**：日常开发集成分支，所有功能分支合并至此
- **推送条件**：本地开发完成，自测无误后可推送
- **注意**：推送前确保代码可运行，不破坏已有功能

## 开发流程

1. 从 `dev` 拉取最新代码
2. 创建功能分支：`feature/<功能名>` 或 `fix/<问题描述>`
3. 在功能分支上开发并本地测试
4. 自测通过后，将功能分支合并到 `dev`
5. 前后端联调通过后，将 `dev` 合并到 `main`

## 提交规范

- 提交信息简洁明了，描述做了什么、为什么做
- 一次提交只做一件事
- 提交前确保代码已格式化且无 lint 错误

## 注意事项

- 不要直接在 `main` 或 `dev` 上开发
- 合并前先拉取目标分支最新代码，解决冲突后再合并
- 有疑问及时沟通，不确定时先问再写
